#summary Details on getMySoldItemAuctionList() API function
#sidebar ApiFunctions

Call this function to get a list of sold auction items


=== Call ===

 * syntax : getMySoldItemAuctionList(string token)

 * parameter : _string_ : secret token

=== Response ===
 * ResponseCategory, giving you a list of sold auction items ID

=== Notification ===
 * None

=== Code example (PHP) ===
{{{
//FUNCTION CALL
$objSoapClient = new SoapClient('http://api.delcampe.net/soap.php?wsdl');
$return = $objSoapClient->authenticateUser('MyPersonalApiKey');

if ($return->status === true) {
  $return = $objSoapClient->getMySoldItemAuctionList($return->data);

  if ($return->status === true) {
    foreach ($return->data as $objItem) {     
      var_export($objItem->data, 1);
    }
  } else {
    var_export($return->errorMsg, 1);
  } 
}
}}}