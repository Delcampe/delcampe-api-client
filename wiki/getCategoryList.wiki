#summary Details on getCategoryList() API function
#sidebar ApiFunctions

Call this function to get the list of Delcampe's categories you can set in [addItemAuction]() or [addItemFixedPrice]().


=== Call ===

 * syntax : getCategoryList(string token, string language, integer parentCategoryID)

 * parameter 1 : _string_ : secret token
 * parameter 2 : _character_ : Optional parameter. The language in wich you want the categories. Default value : 'E'. You can specify one of the following values :
  * 'E' : english
  * 'F' : français
  * 'I' : italiano
  * 'S' : español
  * 'G' : german
  * 'N' : nederlands
 * parameter 3 : _integer_ : Optional parameter. The parent category ID. If you don't specify a parent category ID, it returns all Delcampe's category tree (more than 18.000 categories). Pay attention to your server performance.

=== Response ===

 * response : _array_ : the immediate response is always an array of 2 elements
  * element 1 : _boolean_ : true (if call ok) or false (if call not ok)
  * element 2 : 
   * if call ok : _array_ : list of Delcampe's categories (regarding parent ID set)
   * if call not ok : _string_ : error message

=== Notification ===
 * None

=== Code example (PHP) ===
{{{
//FUNCTION CALL
$objSoapClient = new SoapClient('http://api.delcampe.net/soap.php?wsdl');
list($success, $token) = $objSoapClient->authenticateUser('MyPersonalApiKey');

if (true === $success) {
  $return = $objSoapClient->getCategoryList($token, 'E', 0);
  if ($return[0] === true) {
    var_export($return[1], 1);
  } else {
    echo 'Function call failed. Error message : ' . $return[1]; 
  }
}
}}}